Install Python:

    sudo apt install python3

    sudo apt install python3-pip

Install packages:

    pip install torch nltk requests flask numpy

To train:

    python
        >> import nltk
        >> nltk.download('punkt')

    python training.py

Run ChatBot:

    python chat.py

Run ChatBot App:

    python app.py
